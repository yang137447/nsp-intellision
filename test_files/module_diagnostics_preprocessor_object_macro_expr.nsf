#define ENABLE_FEATURE
#define FEATURE_LEVEL (ENABLE_FEATURE + 1)

float4 PreprocObjectMacroExprMain(float2 uv : TEXCOORD0) : SV_Target0
{
#if FEATURE_LEVEL >= 2
    float2 bad = float4(0.0, 0.0, 0.0, 0.0);
    return float4(uv, bad.x, 1.0);
#else
    return float4(uv, 0.0, 1.0);
#endif
}
