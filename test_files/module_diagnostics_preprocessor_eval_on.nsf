#define ENABLE_GBUFFER_VELOCITY 1

float4 PreprocEvalOn(float2 uv : TEXCOORD0) : SV_Target0
{
#if ENABLE_GBUFFER_VELOCITY
    float2 bad = float4(0.0, 0.0, 0.0, 0.0);
    return float4(uv, bad.x, 1.0);
#else
    return float4(uv, 0.0, 1.0);
#endif
}
