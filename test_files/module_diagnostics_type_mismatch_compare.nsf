float4 CompareMismatchMain(float2 uv : TEXCOORD0) : SV_Target0
{
    float3 a = float3(uv, 0.0);
    float4 b = float4(uv, 0.0, 1.0);
    bool c = a == b;
    return c ? float4(uv, 0.0, 1.0) : float4(uv, 1.0, 0.0);
}
